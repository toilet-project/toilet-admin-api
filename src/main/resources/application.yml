
server:
  port: 8080

spring:
  application:
    name: toilet-admin

  # 1. ?????? ?? ??
  datasource:
    # ?? PC -> ?? PC MySQL ??
    url: jdbc:mysql://192.168.0.100:3306/toilet_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: luha
    password: dl23235656!
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 2. JPA ?? (???? ???? DB ???? ?? ??)
  jpa:
    hibernate:
      ddl-auto: update           # ?? ? ??? ????? DB ???? ??
    show-sql: true              # ???? SQL ??? ?? ??? ??
    properties:
      hibernate:
        format_sql: true        # SQL ??? ?? ??? ???
        dialect: org.hibernate.dialect.MySQLDialect